class DijkstrasShortestPath:

    def shortest_path(self):
        # x = {s}  # vertices processed so far
        # A[s] = 0  # shortest path to vertex

        # while ...
        # grow x by one node
        # swallow the 'one hop away' node that is smallest route

        # tail in x, head outside of x